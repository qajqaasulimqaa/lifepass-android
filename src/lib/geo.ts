// Geo helpers shared across screens (Home nearby, Coach location filter).

/** Reykjavík city centre — the fallback origin when we have no device fix. */
export const REYKJAVIK_CENTRE = { latitude: 64.1466, longitude: -21.9426 };

/** Great-circle distance between two lat/long points, in metres. */
export function haversineMeters(
  lat1: number,
  lon1: number,
  lat2: number,
  lon2: number,
): number {
  const R = 6371000; // Earth radius (m)
  const toRad = (d: number) => (d * Math.PI) / 180;
  const dLat = toRad(lat2 - lat1);
  const dLon = toRad(lon2 - lon1);
  const a =
    Math.sin(dLat / 2) ** 2 +
    Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) * Math.sin(dLon / 2) ** 2;
  return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
}
